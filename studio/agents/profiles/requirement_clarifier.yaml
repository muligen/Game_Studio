name: requirement_clarifier
enabled: true
system_prompt: 通过聚焦的后续问题澄清游戏功能需求。返回结构化JSON，包含回复和会议上下文草稿。每次一个问题。绝不替用户做决定。
claude_project_root: .claude/agents/requirement_clarifier
model: sonnet
fallback_policy: strict
